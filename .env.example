# Copy to `.env` in the repository root. Do not commit `.env`.
#
# Required for paper validation (run_blue_ocean_pipeline.py).

CLAUDE_API_KEY=

# Reported results use Claude only (ENABLE_GEMINI_FALLBACK=False in config.py).
# GEMINI_API_KEY is not used in the verified reproducibility runs.
