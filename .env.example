# Copy to `.env` in the repository root. Do not commit `.env`.
#
# Required for LLM validation (Anthropic Claude primary; Gemini optional fallback).

CLAUDE_API_KEY=

# Optional fallback if Claude is unavailable
GEMINI_API_KEY=
